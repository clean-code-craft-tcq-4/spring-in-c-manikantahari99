name: Build and Run

on: [push, pull_request]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: build
      run: g++ -Wall -Wextra -DCATCH_CONFIG_NO_POSIX_SIGNALS -o testchecker *.c*
      
    - name: run
      run: ./testchecker
